{
    "name": "dynamic-og-worker",
    "version": "1.0.0",
    "devDependencies": {
        "wrangler": "^4.0.0"
    },
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    }
}
